# =============================================================================
# .envs/.env.example — SQLite (local development, default)
# =============================================================================
# Copy this file to .envs/.env.local and adjust as needed.
# This is the default profile: fast local dev with SQLite, no server needed.
#
# Usage:
#   composer dev       →  loads this file and starts the server on :8000
# =============================================================================

APP_ENV=development
APP_DEBUG=true
APP_KEY=base64:CHANGE_ME_run_openssl_rand_-base64_32

# SQLite — default. Leave DB_DATABASE empty to use database/database.sqlite
DB_CONNECTION=sqlite
# DB_DATABASE=/absolute/path/to/database/database.sqlite

CORS_ALLOWED_ORIGINS=*
LOG_LEVEL=debug
