# =============================================================================
# .envs/.env.pg.example — PostgreSQL (local development with Postgres)
# =============================================================================
# Copy this file to .envs/.env.pg.local and fill in your values.
# Use this profile when you want to develop against a local PostgreSQL server.
#
# Usage:
#   composer dev:pg    →  loads this file and starts the server on :8000
# =============================================================================

APP_ENV=development
APP_DEBUG=true
APP_KEY=base64:CHANGE_ME_run_openssl_rand_-base64_32

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=myapp_dev
DB_USERNAME=postgres
DB_PASSWORD=
# DB_SSLMODE=prefer

# Heroku/Railway: set DATABASE_URL to override all DB_* vars above
# DATABASE_URL=pgsql://user:pass@host:5432/dbname?sslmode=require

CORS_ALLOWED_ORIGINS=*
LOG_LEVEL=debug
